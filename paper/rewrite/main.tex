\documentclass[11pt,oneside]{article}

\usepackage[a4paper,top=23mm,bottom=25mm,left=24mm,right=24mm,headheight=22pt]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{newtxtext}
\usepackage{microtype}
\usepackage{setspace}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{amsthm}
\usepackage{newtxmath}
\usepackage{booktabs,tabularx,longtable,array,multirow}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc,backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[mathlines,modulo]{lineno}
\usepackage{xcolor}
\usepackage{xurl}
\usepackage{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\usepackage{caption}
\usepackage[section]{placeins}
\usepackage{seqsplit}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage[most]{tcolorbox}

\makeatletter
\renewcommand{\theHALG@line}{\thealgorithm.\arabic{ALG@line}}
\makeatother

\definecolor{ink}{HTML}{22252A}
\definecolor{accent}{HTML}{493C78}
\definecolor{accenttwo}{HTML}{536B86}
\definecolor{softaccent}{HTML}{F1EFF8}
\definecolor{softblue}{HTML}{EEF3F7}
\definecolor{softgray}{HTML}{F5F5F6}
\definecolor{rulegray}{HTML}{D5D7DB}
\definecolor{warning}{HTML}{8A4B28}

\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accenttwo,
  citecolor=accent,
  pdftitle={Trahens: A Privacy-Enabled Route Discovery Protocol},
  pdfauthor={Andrea Benetton},
  pdfsubject={Current Trahens protocol draft with U1, E1, C1, M1, and W2 profiles}
}

\setstretch{1.045}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.48em plus 0.08em minus 0.04em}
\setlist[itemize]{leftmargin=1.45em,itemsep=0.22em,topsep=0.25em}
\setlist[enumerate]{leftmargin=1.65em,itemsep=0.28em,topsep=0.3em}
\raggedbottom

\titleformat{\section}{\Large\bfseries\color{accent}}{\thesection}{0.65em}{}
\titleformat{\subsection}{\large\bfseries\color{ink}}{\thesubsection}{0.6em}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{accenttwo}}{\thesubsubsection}{0.55em}{}
\titlespacing*{\section}{0pt}{1.6em}{0.55em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.38em}
\titlespacing*{\subsubsection}{0pt}{0.9em}{0.28em}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{accent}Trah\={e}ns Protocol}
\fancyhead[R]{\small\color{accenttwo}Protocol draft -- 30 July 2026}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.35pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{rulegray}\leaders\hrule height \headrulewidth\hfill}}

\captionsetup{font=small,labelfont={bf,color=accent},skip=6pt}

\newcommand{\ProtoName}{Trah\={e}ns}
\newcommand{\concat}{\mathbin\Vert}
\newcommand{\sample}{\xleftarrow{\$}}
\newcommand{\Zq}{\mathbb{Z}_q}
\newcommand{\Group}{\mathbb{G}}
\newcommand{\Identity}{\mathcal{O}}
\newcommand{\Encode}{\operatorname{Encode}}
\newcommand{\Hash}{\operatorname{H}}
\newcommand{\Seal}{\operatorname{Seal}}
\newcommand{\Open}{\operatorname{Open}}
\newcommand{\Rerand}{\operatorname{Rerand}}
\newcommand{\KDF}{\operatorname{KDF}}
\newcommand{\RouteT}{\mathsf{RouteT}}
\newcommand{\BranchT}{\mathsf{BranchT}}
\newcommand{\CandidateT}{\mathsf{CandidateT}}
\newcommand{\Uone}{\ensuremath{\mathsf{U1}}}
\newcommand{\Eone}{\ensuremath{\mathsf{E1}}}
\newcommand{\Cone}{\ensuremath{\mathsf{C1}}}
\newcommand{\Mone}{\ensuremath{\mathsf{M1}}}
\newcommand{\Wtwo}{\ensuremath{\mathsf{W2}}}
\newcommand{\bytes}[1]{\langle #1\rangle}
\newcommand{\code}[1]{\texttt{#1}}

\newtheorem{definition}{Definition}
\newtheorem{property}{Property}
\newtheorem{proposition}{Proposition}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}

\newtcolorbox{statusbox}{
  enhanced,breakable,colback=softgray,colframe=rulegray,
  boxrule=0.45pt,arc=1.2mm,left=2.2mm,right=2.2mm,top=1.6mm,bottom=1.6mm
}
\newtcolorbox{claimbox}[1][]{
  enhanced,breakable,colback=softblue,colframe=accenttwo,
  boxrule=0.55pt,arc=1.2mm,left=2.4mm,right=2.4mm,top=1.7mm,bottom=1.7mm,
  title=#1,fonttitle=\bfseries,coltitle=ink
}
\newtcolorbox{warningbox}[1][]{
  enhanced,breakable,colback=softaccent,colframe=warning,
  boxrule=0.55pt,arc=1.2mm,left=2.4mm,right=2.4mm,top=1.7mm,bottom=1.7mm,
  title=#1,fonttitle=\bfseries,coltitle=warning
}

\title{\vspace{-1.2em}\ProtoName\\[0.25em]\Large A Privacy-Enabled Route Discovery Protocol}
\author{Andrea Benetton\\BlueTeam O\"U, Tallinn, Estonia\\\texttt{andrea.benetton@blueteam.ee}}
\date{Protocol draft -- 30 July 2026}

\begin{document}
\maketitle
\vspace{-1.0em}

\begin{statusbox}
\textbf{Document status.} This document specifies the current research draft of the \ProtoName{} protocol. It defines bounded route discovery, deterministic route-state activation, a concrete classical cryptographic profile, canonical variable-length logical messages, and fixed-size authenticated adjacent-link cells with bounded reassembly. The protocol has not received independent cryptographic review and is not suitable for production deployment. In particular, the active-tagging result in \Cref{sec:active-tagging} blocks any active-adversary unlinkability claim.
\end{statusbox}

\begin{abstract}
Route discovery can disclose endpoint relationships, local topology, and stable correlation handles before an anonymous data path is established. \ProtoName{} is a privacy-enabled control-plane protocol that discovers an eligible responder within a bounded graph radius and installs hop-local bidirectional forwarding state without transmitting a complete route or a network-wide discovery identifier.

The protocol binds five profiles. \Uone{} requires every forwarded branch to replace its adjacent capability, additively tweak its reply key, universally rerandomize its destination-eligibility capsule, reconstruct the logical message, and cross a declared mixing boundary. \Eone{} defines half-open deadlines, equal-time event precedence, candidate windows, delayed responses, cancellation races, tentative state, forward \textsc{Commit}, reverse \textsc{Ready}, and deterministic cleanup. \Cone{} instantiates the cryptographic operations with the RFC~9496 \code{ristretto255} group, a Golle--Jakobsson--Juels--Syverson universal re-encryption construction, an additively tweakable reply KEM, HKDF-SHA-256, ChaCha20-Poly1305, and Ed25519. \Mone{} defines one canonical variable-length representation for each logical control message without semantic padding. \Wtwo{} fragments an M1 message into one or more fixed 1,052-byte adjacent-link cells, authenticates every cell independently, and reassembles the message under explicit byte, fragment, concurrency, and time limits.

This separation avoids treating a fixed transmission unit as the logical message format. A short control message usually occupies one cell, whereas a deeply nested candidate may occupy several. Individual cell length no longer reveals message type or exact logical length, but cell count and timing remain observable unless a separate scheduling profile conceals them. The integrated reference model executes cryptography, canonical encoding, fragmentation, reassembly, and route-state transitions in one event lifecycle. It demonstrates clean multi-cell route activation, bounded cleanup after fragment loss, and rejection of malformed or unauthenticated cells before route-state allocation.

The analysis also identifies a persistent algebraic ratio tag in the eligibility capsule: a compromised relay can create a relation that survives honest rerandomization and can be recognized by a colluding downstream relay. Active-adversary unlinkability is therefore not claimed. The results constitute a precise, falsifiable research baseline rather than a security proof or production benchmark.
\end{abstract}
\linenumbers
\modulolinenumbers[5]

\section{Introduction}
\label{sec:introduction}

\subsection{Motivation}

Anonymous communication is commonly described in terms of a protected data path. Before that path exists, however, a control plane must locate a destination or service, discover one or more usable routes, and install forwarding state. That preparatory process may disclose the same information that the eventual anonymity mechanism is intended to hide: which endpoint is sought, which relays are topologically close, which messages belong to one exploration, and which reverse response corresponds to which forward branch.

The problem is especially visible in decentralized and path-aware networks. A conventional routing protocol may distribute a global or regional topology view. A source-routing system may return a complete path to the sender. A directory may provide a stable lookup handle. Each design can be operationally sound while creating a durable correlation surface. \ProtoName{} investigates a different structure: relays maintain only adjacent capabilities, the initiator receives candidates rather than a topology map, and every branch representation is independently transformed at each forwarding hop.

Privacy cannot be inferred from encryption alone. A stable token inside an encrypted link becomes visible to a compromised relay; variable ciphertext length reveals the length of the padded plaintext; a multi-cell burst may reveal a candidate's approximate depth; a deterministic retry handle links expanding rings; a route may be activated before end-to-end confirmation; and a malleable ciphertext may permit active tagging even though every honest rerandomization changes its bytes. The protocol therefore treats privacy, lifecycle correctness, resource safety, logical encoding, cell framing, reassembly, scheduling, and active-adversary behavior as separate objects of specification and measurement.

\subsection{Contributions}

This paper makes the following contributions:

\begin{enumerate}
  \item It defines a bounded route-discovery control plane in which logical discoveries and expanding-ring policy remain local to the initiator, while relays see only peer-bound branch contexts.
  \item It separates seven properties that must not be conflated: per-cell length equality, passive wire-image unlinkability, conditional batch-local message unlinkability, lifecycle correctness, reassembly safety, active-adversary unlinkability, and traffic-flow unlinkability.
  \item It specifies the \Uone{} branch-local transformation: branch, candidate, cancellation, and route capabilities are replaced at every hop; reply keys are tweaked; eligibility capsules are rerandomized; and outgoing logical messages are reconstructed.
  \item It specifies the \Eone{} event lifecycle, including half-open deadlines, deterministic equal-time precedence, delayed candidates, cancellation races, tentative mappings, \textsc{Commit}, \textsc{Ready}, and cleanup after loss.
  \item It defines the concrete \Cone{} research suite with distinct eligibility, responder-signing, branch-reply, and adjacent-link key roles, canonical encodings, transcript domains, and generic failure behavior.
  \item It defines \Mone{} as a canonical variable-length logical-message codec and \Wtwo{} as a fixed-size encrypted-cell profile with deterministic fragmentation and bounded out-of-order reassembly.
  \item It formalizes the boundary between per-cell length equality and message-size leakage: a message of length $L$ occupies $\lceil L/992\rceil$ W2 cells unless a scheduling profile pads or obscures that count.
  \item It integrates \Uone{}, \Eone{}, \Cone{}, \Mone{}, and \Wtwo{} in one deterministic event model that performs cryptographic, codec, fragmentation, and reassembly operations before route-state activation.
  \item It gives an executable active-tagging counterexample, states the resulting claim boundary, and identifies replacement of the eligibility construction as a mandatory security gate.
\end{enumerate}

\subsection{Scope and reading guide}

\begin{claimbox}[Scope of this document]
\ProtoName{} discovers one or more eligible responders within a bounded graph radius and establishes hop-local bidirectional forwarding state to one selected responder. It does not define global name resolution, inter-domain routing policy, application-data congestion control, economic incentives, a new physical or link layer, or a replacement for IP.
\end{claimbox}

\Cref{sec:background} relates the design to anonymity and cryptographic mechanisms. \Cref{sec:model} states the network and adversary models. \Cref{sec:notation} fixes the notation and key roles. \Cref{sec:overview} explains the complete protocol before formal cryptography. \Cref{sec:c1,sec:forward,sec:reverse,sec:lifecycle,sec:messages} define the cryptographic, operational, logical-message, cell-framing, and reassembly mechanisms. \Cref{sec:unlinkability,sec:resources} state the privacy and abuse boundaries. \Cref{sec:evaluation} reports deterministic evidence. The appendices summarize normative requirements and conformance values.

\section{Related work and cryptographic context}
\label{sec:background}

\subsection{Network-layer anonymity and mix packets}

HORNET demonstrates high-speed anonymous forwarding with symmetric cryptography on the data-plane fast path and no per-flow forwarding state in intermediate routers~\cite{hornet}. TARANET adds setup mixing and coordinated constant-rate transmission to resist traffic analysis at the network layer~\cite{taranet}. Sphinx provides a compact mix packet format with per-hop transformation, path-length hiding, reply support, and formal security goals~\cite{sphinx}. Loopix combines Sphinx packets with stochastic mixing and cover traffic~\cite{loopix}.

These systems begin with a route or layered packet structure already selected by the sender or supplied by another mechanism. \ProtoName{} addresses a prior problem: the sender initially knows the endpoint descriptor but not the path. The protocol must explore a branching graph while avoiding a stable cross-hop identifier. Consequently, techniques designed for a single precomputed onion cannot be copied without modification.

\subsection{Universal re-encryption}

Ordinary rerandomizable ElGamal permits ciphertext rerandomization when the public key is known. Universal re-encryption (URE) additionally permits a mixer to transform a ciphertext without knowing which public key protects it~\cite{gjjs}. That property is useful for \ProtoName{} because including the destination public key beside each discovery capsule would itself create a stable selector. The Golle--Jakobsson--Juels--Syverson construction doubles the ElGamal representation: one pair carries the message and a second pair carries an encryption of the identity, enabling public-key-free rerandomization.

Security terminology for URE has evolved. Recent work emphasizes encryption anonymity, minimal security notions, and composable application behavior rather than rerandomization correctness alone~\cite{ure2023}. C1 therefore treats the 2004 construction as a concrete research candidate and states its claim boundary. It does not infer active security merely because the equations are correct.

\subsection{HPKE and the custom reply KEM}

HPKE standardizes hybrid public-key encryption from a KEM, KDF, and AEAD and gives precise context binding and test vectors~\cite{hpke}. The C1 reply construction follows that discipline but requires a public key that can be additively tweaked at each hop and whose corresponding secret can later be reconstructed by the initiator. RFC~9180 does not register a \code{ristretto255} KEM with this property. C1 therefore defines an HPKE-inspired custom KEM and explicitly refrains from claiming RFC~9180 interoperability.

\section{System model, adversary, and claims}
\label{sec:model}

\subsection{Network model}

Let the communication substrate be an undirected graph
\begin{equation}
G=(V,E),
\label{eq:graph}
\end{equation}
where a node $n\in V$ may act as initiator, relay, responder, or any combination of these roles. An edge $(u,v)\in E$ denotes an authenticated adjacent-link association. A route of length $d$ is a sequence
\begin{equation}
\rho=(n_0,n_1,\ldots,n_d), \qquad (n_i,n_{i+1})\in E.
\label{eq:route}
\end{equation}
The initiator is $n_0$ and the responder is $n_d$. The protocol never transmits $\rho$ as one object.

The adjacent-link transport supplies peer authentication, confidentiality and integrity, exact cell-record boundaries, a directional link epoch, a monotonically managed sequence, and notification of connection failure. W2 fixes the complete cell-record length while M1 remains a variable-length logical representation. A separate scheduling profile defines cell batching, fragment interleaving, release delay, cell-count padding, and cover traffic. Adjacent-link protection prevents an external observer from reading or modifying an honest-to-honest edge; it does not make a compromised relay honest or constrain the content of new cells emitted by that relay.

\subsection{Adversary}

The adversary may:

\begin{itemize}
  \item observe links not protected by the stated underlay profile;
  \item compromise a bounded set of nodes and inspect their keys, state, queues, and local timing;
  \item inject, drop, delay, duplicate, reorder, redirect, or modify records at compromised nodes;
  \item generate fresh syntactically valid branches rather than only replaying old records;
  \item coordinate observations from multiple compromised relays;
  \item attempt resource exhaustion, candidate spam, and active ciphertext tagging.
\end{itemize}

The adversary cannot break the selected cryptographic assumptions, forge uncompromised adjacent-link authentication, or read an honest-to-honest protected link. Traffic-flow claims require a separate observation and scheduling profile; C1 alone does not conceal timing.

\subsection{Separated properties}

\begin{definition}[Per-cell length equality]
Every W2 adjacent-link cell has the same complete length, independently of logical message type, candidate depth, fragment position, protected-body length, or chaff status.
\end{definition}

\begin{definition}[Canonical logical-message representation]
For every accepted semantic control message, M1 admits exactly one byte representation. Length integers are minimal, fragmentation metadata is absent from the logical message, and no trailing bytes or semantic padding are permitted.
\end{definition}

\begin{definition}[Passive wire-image unlinkability]
For passively observed messages processed by at least one honest relay, protocol fields exposed after adjacent-link decryption do not provide a deterministic equality test linking an incoming representation to a non-adjacent outgoing representation.
\end{definition}

\begin{definition}[Batch-local message unlinkability]
After an honest per-hop cryptographic transformation and a mixing boundary containing $k\ge2$ cells with the same observable treatment, a passive observer cannot match a designated input logical message to its output with advantage exceeding the relevant cryptographic advantage and information exposed by cell count and scheduling.
\end{definition}

\begin{definition}[Lifecycle correctness]
Every accepted control event produces one deterministic state transition or rejection, every state has a finite local deadline, and loss of any cleanup message cannot create permanent state.
\end{definition}

\begin{definition}[Reassembly safety]
An adversarial stream of authenticated or unauthenticated fragments cannot make a node allocate route-semantic state before complete canonical M1 decoding, and incomplete-message memory is bounded by explicit context, byte, fragment, and time limits.
\end{definition}

\begin{definition}[Active-adversary unlinkability]
Compromised relays cannot install a persistent recognizable relation, selective-failure marker, or other active tag that links non-adjacent representations of one branch with non-negligible advantage.
\end{definition}

\begin{definition}[Traffic-flow unlinkability]
An observer cannot reliably correlate communication endpoints using timing, direction, volume, cell count, fragment bursts, queue behavior, topology, retries, or long-lived observation.
\end{definition}

\begin{table}[ht]
\centering
\caption{Protocol claim matrix}
\label{tab:claims}
\begin{tabularx}{\textwidth}{@{}p{0.25\textwidth}p{0.19\textwidth}X@{}}
\toprule
Property & Status & Required evidence or dependency \\
\midrule
Per-cell length equality & Specified and tested & Every W2 transmission is one 1,052-byte adjacent-link record. \\
Canonical M1 representation & Specified and tested & Minimal varints, exact body containment, no trailing bytes, and deterministic fragmentation. \\
Passive wire-image unlinkability & Structural claim & No stable cross-hop field; per-hop token replacement, key tweak, URE rerandomization, M1 reconstruction, fresh W2 identifiers, padding, and link encryption. \\
Batch-local unlinkability & Conditional claim & C1 assumptions, at least one honest transform, W2 cell equality, and a declared interleaving or mixing boundary. \\
Lifecycle correctness & Model-supported claim & E1 state rules, integrated authentication, bounded event simulation, and zero final route state. \\
Reassembly safety & Model-supported claim & Fixed maximum message and fragment count, bounded contexts and reserved bytes, conflict invalidation, and local timeout. \\
Active-adversary unlinkability & Not claimed & The persistent ratio-tag counterexample in \Cref{sec:active-tagging} violates the property. \\
Traffic-flow unlinkability & Not claimed & A scheduling profile, cell-count padding, cover traffic, and correlation experiments are required. \\
Production cryptographic security & Not claimed & Independent review, proof alignment, side-channel analysis, and implementation audit are absent. \\
\bottomrule
\end{tabularx}
\end{table}
\FloatBarrier

\section{Terms and notation}
\label{sec:notation}

The notation assigns each key role and capability a distinct alphabet. The graph-edge symbol $E$ is never used for a cryptographic value, and a capability symbol denotes only one direction and one adjacent scope.

\begin{table}[H]
\centering
\caption{Principal notation}
\label{tab:notation}
{\small
\begin{tabularx}{\textwidth}{@{}p{0.18\textwidth}p{0.25\textwidth}X@{}}
\toprule
Symbol & Class & Meaning \\
\midrule
$G=(V,E)$ & network & Undirected graph, nodes, and authenticated adjacent associations. \\
$\rho=(n_0,\ldots,n_d)$ & route & Conceptual route; never transmitted as a complete object. \\
$\Group,q,B,\Identity$ & C1 group & Prime-order \code{ristretto255} group, order, generator, and identity. \\
$(a,A=aB)$ & endpoint eligibility & Long-term key pair used only to recognize the eligibility marker. \\
$(sk_S,vk_S)$ & endpoint signing & Ed25519 pair used to authenticate responder candidate transcripts. \\
$(x_i,X_i=x_iB)$ & branch reply & Ephemeral reply key at depth $i$ on one branch. \\
$\delta_i$ & reply tweak & Relay scalar satisfying $X_{i+1}=X_i+\delta_iB$. \\
$Q_i$ & eligibility capsule & Four-point URE ciphertext after $i$ honest or malicious transformations. \\
$\tau_i$ & branch capability & Fresh token meaningful only on one adjacent link and epoch. \\
$\gamma_i$ & candidate capability & Fresh reverse candidate token meaningful only on one adjacent link. \\
$\lambda_i^{\rightarrow}$ & route capability & Parent-facing forward label for \textsc{Commit} and data. \\
$\lambda_i^{\leftarrow}$ & route capability & Child-facing reverse label for \textsc{Ready} and reverse data. \\
$C_i$ & reverse capsule & Candidate capsule encrypted to reply key $X_i$. \\
$M$ and $|M|$ & M1 message & Canonical logical-message byte string and its length, $1\le |M|\le16{,}384$. \\
$P_C=992$ & W2 payload & Maximum M1 fragment bytes in one cell. \\
$h_C=32$ & W2 header & Encrypted per-cell fragmentation header length. \\
$L_C=1052$ & W2 record & Complete adjacent-link cell-record length, $12+1024+16$ bytes. \\
$\mu$ & W2 local ID & Fresh 128-bit identifier scoped to one message on one directional link epoch. \\
$q(M)$ & cell count & Canonical value $\lceil |M|/992\rceil$, with $1\le q(M)\le17$. \\
$R[\ell,\mu]$ & reassembly & Context for authenticated link scope $\ell$ and local identifier $\mu$. \\
$[t_c,t_e)$ & lifetime & Half-open local validity interval under E1. \\
$\mathcal B$ & budget & Limits for messages, cells, state, bytes, reassembly, cryptographic work, and time. \\
$(e_\ell,s_\ell)$ & link scope & Public directional epoch and sequence in the 12-byte cell-record header. \\
\bottomrule
\end{tabularx}}
\end{table}

\begin{table}[ht]
\centering
\caption{Key-role separation in C1}
\label{tab:keyroles}
\begin{tabularx}{\textwidth}{@{}p{0.19\textwidth}p{0.20\textwidth}p{0.24\textwidth}X@{}}
\toprule
Key & Lifetime & Known by & Prohibited reuse \\
\midrule
Eligibility $(a,A)$ & Long-term endpoint & Endpoint; descriptor exposes $A$ & Signing, reply encryption, link encryption. \\
Signing $(sk_S,vk_S)$ & Long-term endpoint & Endpoint; descriptor exposes $vk_S$ & Group scalar or eligibility secret. \\
Reply $(x_i,X_i)$ & One branch setup & Initiator eventually; $X_i$ on one branch hop & Another first-hop branch or another logical discovery. \\
Link traffic key & One link epoch & Adjacent peers & End-to-end responder authentication. \\
\bottomrule
\end{tabularx}
\end{table}

\section{Protocol overview}
\label{sec:overview}

\subsection{Phase structure}

\Cref{fig:phases} separates route discovery from route activation. The initiator may start several local expanding rings, but ring identity is never transmitted. A responder returns a candidate through stored branch contexts. Candidate return installs only tentative route state. The selected route becomes usable only after \textsc{Commit} travels forward and \textsc{Ready} returns.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
  node distance=7.5mm and 7mm,
  box/.style={draw=accent,rounded corners=1.4mm,fill=softaccent,minimum height=8mm,minimum width=27mm,align=center,font=\small\bfseries},
  smallbox/.style={draw=accenttwo,rounded corners=1.2mm,fill=softblue,minimum height=7mm,minimum width=25mm,align=center,font=\small},
  arr/.style={-{Latex[length=2.2mm]},thick,color=accenttwo}
]
\node[box] (discover) {DISCOVER\\branch expansion};
\node[box,right=of discover] (candidate) {CANDIDATE\\tentative return};
\node[box,right=of candidate] (commit) {COMMIT\\capacity reserve};
\node[box,right=of commit] (ready) {READY\\route activation};
\draw[arr] (discover) -- (candidate);
\draw[arr] (candidate) -- (commit);
\draw[arr] (commit) -- (ready);
\node[smallbox,below=10mm of discover] (branch) {Branch state};
\node[smallbox,below=10mm of candidate] (tentative) {Tentative route};
\node[smallbox,below=10mm of commit] (pending) {Pending-ready};
\node[smallbox,below=10mm of ready] (active) {Active route};
\draw[arr] (branch) -- (tentative);
\draw[arr] (tentative) -- (pending);
\draw[arr] (pending) -- (active);
\end{tikzpicture}
\caption{Protocol phases and the corresponding relay-state progression. Every state also has an independent expiry path not shown in the diagram.}
\label{fig:phases}
\end{figure}

\subsection{Local state versus wire state}

The initiator maintains a \emph{logical discovery}: application request, destination descriptor, ring schedule, cumulative budget, candidate set, and selection deadline. None of its identity fields appears on the wire. The only relay-visible discovery object is a \emph{branch context}, bound to one ingress peer, one branch token, one link epoch, one incoming reply public key, one URE capsule, a finite expiry, and bounded child mappings.

This distinction permits retries without a network-wide attempt identifier. It also creates a cost: a relay cannot collapse two converging branches merely because they belong to the same logical discovery. That loss of global duplicate suppression is deliberate and is bounded by explicit quotas.

\subsection{One branch in plain language}

For one outgoing branch, the initiator generates a random link-local token, an ephemeral reply key, and a URE capsule addressed to the endpoint eligibility key. A relay admits a branch only after complete W2 reassembly and canonical M1 decoding. For each selected child it generates a new token, adds a random scalar multiple of the group generator to the reply key, universally rerandomizes the four-point eligibility capsule, reconstructs the M1 message, assigns a new link-local W2 message identifier, fragments the message canonically, and sends freshly padded adjacent-link cells. The child cannot compare a stable token, reply key, destination ciphertext, message identifier, or received padding with the parent's input.

An eligible endpoint decrypts the URE capsule to a fixed marker. It signs a candidate transcript and encrypts the candidate to the final reply key. On the reverse path each relay encrypts its stored key tweak and the child capsule to the prior reply key. The initiator opens one layer, learns the tweak, derives the next secret key, and repeats until it verifies the responder.

\section{Concrete cryptographic profile C1}
\label{sec:c1}

\subsection{Primitive suite and status}

\begin{table}[ht]
\centering
\caption{C1 primitive suite}
\label{tab:c1suite}
\begin{tabularx}{\textwidth}{@{}p{0.25\textwidth}p{0.31\textwidth}X@{}}
\toprule
Function & Primitive & Rationale and status \\
\midrule
Prime-order group & \code{ristretto255}, RFC~9496 & Canonical 32-byte group abstraction suitable for higher-level protocols~\cite{ristretto}. \\
Hash and KDF & SHA-256, SHA-512, HKDF-SHA-256 & Domain-separated transcript hashes and extract-and-expand derivation~\cite{hkdf}. \\
AEAD & ChaCha20-Poly1305 & 256-bit key, 96-bit nonce, full 128-bit tag~\cite{chacha}. \\
Signature & Ed25519 & Responder authentication with standard raw key and signature encodings~\cite{eddsa}. \\
Eligibility & GJJS-style URE & Concrete and universally rerandomizable, but malleable and not independently reviewed in this profile~\cite{gjjs,ure2023}. \\
Reply KEM & \code{TR-KEM-R255} & HPKE-inspired custom KEM with additive public-key tweaks; research use only. \\
\bottomrule
\end{tabularx}
\end{table}

\begin{warningbox}[Cryptographic gate]
C1 resolves ambiguity and enables interoperability tests. It does not close the production-security gate. The URE construction requires modern proof alignment and active-tagging analysis; the reply KEM is custom and unregistered; the complete suite is classical rather than post-quantum.
\end{warningbox}

\subsection{Canonical encodings and domain separation}

Every C1 group element has the canonical 32-byte encoding defined by RFC~9496. Scalars are canonical 32-byte little-endian integers less than $q$; zero is rejected where a secret, tweak, or ephemeral scalar is required. Public keys and KEM encapsulations reject the identity element.

Let \code{LP16(x)} be a two-byte unsigned big-endian length followed by $x$. C1 defines
\begin{equation}
\operatorname{EF}(\ell;x_1,\ldots,x_m)
 = \code{``Trahens-C1-v1''}\concat \operatorname{LP16}(\ell)
   \concat \operatorname{LP16}(x_1)\concat\cdots\concat\operatorname{LP16}(x_m).
\label{eq:encodefields}
\end{equation}
All KDF and transcript labels use \cref{eq:encodefields}. Implicit concatenation, JSON serialization, locale-dependent text, and alternate integer encodings are non-conforming.

\subsection{Endpoint descriptor and address}

An endpoint has independent eligibility and signing key pairs:
\begin{equation}
(a,A=aB), \qquad (sk_S,vk_S).
\end{equation}
The 67-byte C1 descriptor is
\begin{equation}
D=\code{version}_{1}\concat\code{suite}_{2}\concat\bytes{A}_{32}\concat vk_{S,32},
\label{eq:descriptor}
\end{equation}
and its address is
\begin{equation}
\mathsf{addr}=\operatorname{SHA256}(\operatorname{EF}(\code{endpoint-address};D)).
\label{eq:address}
\end{equation}
A discovery initiator needs the descriptor, not merely the address hash, because it must encrypt to $A$ and later verify signatures under $vk_S$. Any directory supplying $D$ must authenticate the binding in \cref{eq:address}.

\subsection{Universal eligibility capsule}
\label{sec:ure}

C1 represents eligibility by one fixed group element $M_\star$, derived from a domain-separated 64-byte hash using the RFC~9496 element-derivation interface. Encrypting a fixed marker is sufficient: only the holder of the intended eligibility secret obtains $M_\star$ after the consistency check; a wrong key produces a failed check or an unrelated group element with overwhelming probability.

\subsubsection{Encryption}

For public key $A=aB$, sample $r_0,r_1\sample\Zq^*$ and compute
\begin{equation}
\begin{aligned}
U_0 &= M_\star+r_0A, & V_0 &= r_0B,\\
U_1 &= r_1A,         & V_1 &= r_1B.
\end{aligned}
\label{eq:ureenc}
\end{equation}
The capsule is
\begin{equation}
Q=((U_0,V_0),(U_1,V_1)),
\end{equation}
encoded as four consecutive 32-byte group elements. It is therefore exactly 128 bytes.

\subsubsection{Public-key-free rerandomization}

A relay samples $s_0\sample\Zq^*$ and $s_1\sample(\Zq^*\setminus\{1\})$, then computes
\begin{equation}
\begin{aligned}
U'_0 &= U_0+s_0U_1, & V'_0 &= V_0+s_0V_1,\\
U'_1 &= s_1U_1,     & V'_1 &= s_1V_1.
\end{aligned}
\label{eq:urerand}
\end{equation}
The relay does not need $A$. For a valid input, the effective randomness becomes $r'_0=r_0+s_0r_1$ and $r'_1=s_1r_1$. The restrictions $s_0\ne0$ and $s_1\ne1$ ensure that all four point encodings change; an implementation rejects an unchanged result.

\begin{proposition}[URE correctness]
For every valid C1 capsule $Q$ encrypted under $A=aB$, decryption of any rerandomization $Q'$ produced by \cref{eq:urerand} yields the same marker $M_\star$ and passes the consistency check.
\end{proposition}

\begin{proof}
Using $U'_1=s_1r_1A$ and $V'_1=s_1r_1B$,
$U'_1-aV'_1=s_1r_1(aB)-a(s_1r_1B)=\Identity$.
Similarly,
$U'_0-aV'_0=M_\star+(r_0+s_0r_1)A-a(r_0+s_0r_1)B=M_\star$.
\end{proof}

\subsubsection{Decryption}

The endpoint computes
\begin{equation}
T=U_1-aV_1
\end{equation}
and rejects unless $T=\Identity$. It then computes $M=U_0-aV_0$ and declares itself eligible only if $M=M_\star$. Invalid encodings, consistency failure, marker mismatch, and wrong-key failure all map to the same local result.

\begin{algorithm}[ht]
\caption{C1 eligibility processing at a potential responder}
\label{alg:eligibility}
\begin{algorithmic}[1]
\Require eligibility secret $a$, encoded capsule $Q$
\If{any of the four group elements fails canonical decoding}
  \State \Return \textsc{Invalid-Crypto}
\EndIf
\State $T\gets U_1-aV_1$
\If{$T\ne\Identity$}
  \State \Return \textsc{Not-Eligible}
\EndIf
\State $M\gets U_0-aV_0$
\If{$M\ne M_\star$}
  \State \Return \textsc{Not-Eligible}
\EndIf
\State \Return \textsc{Eligible}
\end{algorithmic}
\end{algorithm}

\subsection{Reply-key tweaking}
\label{sec:tweak}

For every first-hop branch, the initiator samples $x_0\sample\Zq^*$ and publishes $X_0=x_0B$. A relay forwarding to a child samples $\delta_i\sample\Zq^*$ and computes
\begin{equation}
X_{i+1}=X_i+\delta_iB.
\label{eq:tweakpublic}
\end{equation}
The relay stores $\delta_i$ only in the corresponding child context. It resamples if the result is the identity. The initiator later computes
\begin{equation}
x_{i+1}=x_i+\delta_i\pmod q.
\label{eq:tweaksecret}
\end{equation}

\begin{proposition}[Tweak consistency]
The secret update in \cref{eq:tweaksecret} corresponds to the public update in \cref{eq:tweakpublic}.
\end{proposition}
\begin{proof}
$(x_i+\delta_i)B=x_iB+\delta_iB=X_i+\delta_iB=X_{i+1}$.
\end{proof}

\subsection{TR-KEM-R255 and reply encryption}
\label{sec:replykem}

To encrypt to $X=xB$, the sender samples $e\sample\Zq^*$ and computes
\begin{equation}
R=eB, \qquad Z=eX.
\label{eq:kemdh}
\end{equation}
The receiver computes the same $Z=xR$. The KEM context binds the suite, encapsulated element, recipient public key, and a caller-provided \code{info} value:
\begin{align}
\mathsf{ctx} &= \operatorname{EF}(\code{reply-kem-context};\code{suite},\bytes R,\bytes X,\mathsf{info}),\\
\mathsf{prk} &= \operatorname{HKDFExtract}(0^{32},\operatorname{EF}(\code{reply-kem-dh};\bytes Z)),\\
\mathsf{ss} &= \operatorname{HKDFExpand}(\mathsf{prk},\operatorname{EF}(\code{reply-kem-secret};\mathsf{ctx}),32).
\end{align}
The AEAD key and nonce are independently labeled HKDF expansions of $\mathsf{ss}$ and $\mathsf{ctx}$. The output is $\bytes R$ followed by a ChaCha20-Poly1305 ciphertext with the complete 16-byte tag.

The nonce is deterministic under the encapsulation-derived secret. This is safe only if $e$ is fresh. Reusing $e$ for the same recipient and \code{info} repeats both key and nonce and is prohibited. Deterministic ephemerals appear only in published test vectors.

\subsection{Responder authentication and transcript binding}

The responder signs
\begin{equation}
\mathsf{CandidateTH}=
\operatorname{SHA256}(\operatorname{EF}(\code{candidate-transcript};\mathcal F)),
\label{eq:candidateth}
\end{equation}
where the ordered field list $\mathcal F$ includes protocol and profile versions, endpoint address and descriptor, final reply public key, offer class, route limits, offer expiry, commit challenge, and responder nonce. The Ed25519 key must equal $vk_S$ in the descriptor, and the descriptor must hash to the requested endpoint address.

The response includes a random commit challenge. Later, \textsc{Commit} and \textsc{Ready} carry end-to-end HMAC proofs over domain-separated transcript hashes. Relays treat those protected bodies as opaque and route them using local labels.

\subsection{Failure normalization}

C1 exposes one state-machine outcome, \textsc{Invalid-Crypto}, for invalid points, identity public keys, non-canonical scalars, URE failure, AEAD failure, AAD or \code{info} mismatch, invalid signatures, and transcript mismatch. The detailed reason may be logged locally but is never sent as a differentiated response. Cell length and rate checks precede adjacent-link authentication. A receiver may perform a non-mutating replay-window precheck from the public link header, but it commits replay state only after authentication; an unauthenticated record cannot reserve a sequence number. Authenticated replay rejection, W2 fragmentation checks, M1 syntax, and capacity admission all precede expensive route cryptography.

This rule limits explicit oracles; it does not prove constant-time behavior. Production code would require side-channel review, constant-time group and comparison operations, memory-clearing policy, and a uniform scheduling analysis.

\section{Forward discovery}
\label{sec:forward}

\subsection{Initiator behavior}

A logical discovery selects a local expanding-ring policy
\begin{equation}
\mathcal R_r=(d_r,f_r^{(0)},f_r,\Delta_r),
\end{equation}
where $d_r$ is the hop limit, $f_r^{(0)}$ the first-hop fan-out, $f_r$ the relay fan-out, and $\Delta_r$ the local candidate window. Neither $r$ nor the tuple is transmitted directly; only bounded propagation and fan-out classes required by the relay appear in the branch record.

For every first-hop peer, the initiator creates independent state. It does not reuse a branch token, root reply key, URE randomness, padding, or adjacent-link ciphertext.

\begin{algorithm}[ht]
\caption{Initiator: create one C1 DISCOVER branch}
\label{alg:init}
\begin{algorithmic}[1]
\Require first-hop peer $n_1$, destination descriptor $D$, local budget $\mathcal B$
\If{$\mathcal B$ cannot reserve one branch and the required cell transmissions}
  \State \Return failure
\EndIf
\State parse and validate $(A,vk_S)$ from $D$; verify the requested address
\State $x_0\sample\Zq^*$; $X_0\gets x_0B$
\State $\tau_0\sample\{0,1\}^{128}$
\State $Q_0\gets\mathsf{URE.Enc}(A,M_\star)$
\State $M_0\gets\Encode(\textsc{Discover},\tau_0,X_0,Q_0,\mathsf{bounds},\mathsf{expiry})$
\State reconstruct and pad $M_0$ to the DISCOVER record class
\State store $(n_1,\tau_0,x_0,\mathsf{expiry})$ in initiator-local branch state
\State enqueue a fresh adjacent-link encryption of $M_0$
\end{algorithmic}
\end{algorithm}

\subsection{Relay validation order}

The relay processes cheap and local checks before expensive group operations:

\begin{enumerate}
  \item record-class length, byte rate, and adjacent-link replay;
  \item adjacent-link authentication and protocol/profile identifiers;
  \item canonical framing, lifetime class, and branch-token replay;
  \item ingress-peer token bucket and node-global work budget;
  \item provisional state reservation;
  \item C1 point decoding, URE validation for rerandomization, and reply-key processing;
  \item full state allocation and bounded outgoing work.
\end{enumerate}

A failure at one stage does not consume resources reserved for a later stage. W2 reassembly state is accounted separately and does not imply route-semantic admission. No invalid branch generates an amplified error response.

\subsection{Per-child transformation}

For each selected child, the relay performs all transformations independently. Reusing one tweak or rerandomization across several children would allow sibling equality tests and is prohibited.

\begin{algorithm}[ht]
\caption{Relay: transform one admitted DISCOVER branch}
\label{alg:relay}
\begin{algorithmic}[1]
\Require ingress peer $p$, token $\tau_i$, key $X_i$, capsule $Q_i$, bounds
\State allocate branch context $b\gets\BranchT[p,\tau_i]$
\State $S\gets\mathsf{SelectChildren}(N(n_i)\setminus\{p\},f)$
\ForAll{$c\in S$}
  \State $\tau_c\sample\{0,1\}^{128}$
  \Repeat
    \State $\delta_c\sample\Zq^*$; $X_c\gets X_i+\delta_cB$
  \Until{$X_c\ne\Identity$}
  \State $s_{0,c}\sample\Zq$; $s_{1,c}\sample\Zq^*$
  \State $Q_c\gets\Rerand(Q_i;s_{0,c},s_{1,c})$
  \State store $(c,\tau_c,\delta_c,X_c)$ in $b$
  \State reconstruct all mutable fields and generate fresh padding
  \State enqueue a fresh adjacent-link encryption to $c$ through the mixing queue
\EndFor
\end{algorithmic}
\end{algorithm}

\subsection{Loops and converging branches}

Immediate backtracking to the ingress peer is excluded. Exact duplicates on the same link epoch are rejected. A branch arriving over another peer or under another token is a different branch context, even if it belongs to the same initiator-local discovery. Longer cycles and converging branches therefore cause repeated state. The hop limit, fan-out, per-peer token bucket, per-node context ceiling, cumulative transmission budget, and expiry bound the amplification.

Attempt-wide first-parent suppression would reduce cost but requires an equality domain shared across hops. the current protocol chooses the privacy structure and measures the resulting cost rather than hiding it.

\section{Candidate return and route activation}
\label{sec:reverse}

\subsection{Responder candidate}

A node first evaluates the C1 eligibility capsule. If eligible and willing to respond, it constructs a candidate payload containing the endpoint descriptor, offer and limit classes, expiry, commit challenge, responder nonce, final reply key, and Ed25519 signature over \cref{eq:candidateth}. It encrypts the payload to $X_d$ using \code{TR-KEM-R255}.

The responder does not reveal its identity to intermediate relays. Those relays see only a fixed-class candidate record, local capabilities, and an opaque reverse capsule.

\subsection{Relay reverse layer}

Suppose relay $n_i$ previously transformed $X_i$ into $X_{i+1}=X_i+\delta_iB$ for the selected child. On receiving the child candidate capsule $C_{i+1}$, the relay creates a fresh parent candidate token $\gamma_i$ and forward label $\lambda_i^{\rightarrow}$, then computes
\begin{equation}
C_i=\Seal_{X_i}\left(
\Encode(\delta_i,C_{i+1},\lambda_i^{\rightarrow},\mathsf{limits},\mathsf{padding});
\mathsf{info}_i,\mathsf{aad}_i
\right).
\label{eq:reversewrap}
\end{equation}
The \code{info} and AAD bind the protocol version, suite, depth class, parent token, parent label, record class, link epoch, and offer expiry. A mutation of those fields causes AEAD failure.

The relay stores a tentative mapping from the parent-facing candidate capability and forward label to the child branch. It does not learn the responder payload or the contents of $C_{i+1}$.

\subsection{Initiator opening procedure}

The initiator begins with $x_0$. After opening $C_0$, it obtains $\delta_0$ and $C_1$, computes $x_1=x_0+\delta_0\bmod q$, and continues. At the innermost layer it verifies the endpoint descriptor, address, Ed25519 signature, final reply key, responder nonce, offer limits, and expiry.

\begin{algorithm}[ht]
\caption{Initiator: open the C1 candidate chain}
\label{alg:open}
\begin{algorithmic}[1]
\Require root secret $x_0$, outer capsule $C_0$, maximum depth $d_{\max}$
\State $i\gets0$
\While{$i\le d_{\max}$}
  \State $Y\gets\Open_{x_i}(C_i;\mathsf{info}_i,\mathsf{aad}_i)$
  \If{$Y$ is a responder candidate}
    \State verify descriptor, address, signature, final $X_i$, nonce, limits, and expiry
    \State \Return authenticated candidate
  \ElsIf{$Y=\Encode(\delta_i,C_{i+1},\Lambda_i)$}
    \State validate canonical non-zero $\delta_i$ and local transcript context
    \State $x_{i+1}\gets x_i+\delta_i\pmod q$
    \State $i\gets i+1$
  \Else
    \State \Return \textsc{Invalid-Crypto}
  \EndIf
\EndWhile
\State \Return \textsc{Invalid-Crypto}
\end{algorithmic}
\end{algorithm}

\subsection{Worked three-hop example}
\label{sec:workedexample}

Consider $n_0\rightarrow n_1\rightarrow n_2\rightarrow n_3$, where $n_3$ is the responder. The initiator creates $(x_0,X_0)$. Relay $n_1$ chooses $\delta_0$ and sends $X_1=X_0+\delta_0B$. Relay $n_2$ chooses $\delta_1$ and sends $X_2=X_1+\delta_1B$. The responder encrypts its candidate to $X_2$.

On the reverse path, $n_2$ seals $(\delta_1,C_2)$ to $X_1$ and $n_1$ seals $(\delta_0,C_1)$ to $X_0$. The initiator opens $C_0$ with $x_0$, derives $x_1=x_0+\delta_0$, opens $C_1$, derives $x_2=x_1+\delta_1$, and finally opens the responder candidate. Neither relay learns $x_0$, any later secret, or the responder payload.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
  n/.style={draw=accent,rounded corners,fill=softaccent,minimum width=18mm,minimum height=8mm,font=\small\bfseries},
  arr/.style={-{Latex[length=2mm]},thick,color=accenttwo},
  rev/.style={{Latex[length=2mm]}-,thick,color=warning},
  lab/.style={font=\scriptsize,align=center,fill=white,inner sep=1.2pt}
]
\node[n] (n0) {$n_0$};
\node[n,right=24mm of n0] (n1) {$n_1$};
\node[n,right=24mm of n1] (n2) {$n_2$};
\node[n,right=24mm of n2] (n3) {$n_3$};
\draw[arr] (n0) -- node[lab,above] {$X_0,Q_0$} (n1);
\draw[arr] (n1) -- node[lab,above] {$X_1,Q_1$\\$X_1=X_0+\delta_0B$} (n2);
\draw[arr] (n2) -- node[lab,above] {$X_2,Q_2$\\$X_2=X_1+\delta_1B$} (n3);
\draw[rev] ([yshift=-9mm]n0.east) -- node[lab,below] {$C_0=\Seal_{X_0}(\delta_0,C_1)$} ([yshift=-9mm]n1.west);
\draw[rev] ([yshift=-9mm]n1.east) -- node[lab,below] {$C_1=\Seal_{X_1}(\delta_1,C_2)$} ([yshift=-9mm]n2.west);
\draw[rev] ([yshift=-9mm]n2.east) -- node[lab,below] {$C_2=\Seal_{X_2}(\mathsf{candidate})$} ([yshift=-9mm]n3.west);
\end{tikzpicture}
\caption{Reply-key evolution and nested candidate return on a three-edge route. Link-local tokens and route labels are omitted for clarity.}
\label{fig:keychain}
\end{figure}

\subsection{Commit and readiness}

A candidate creates only tentative state. The initiator selects at most one candidate and sends \textsc{Commit} through its first-hop forward label. Each relay validates the peer, label, route generation, local transcript digest, and capacity class; reserves route capacity; changes the mapping to \textsc{Pending-Ready}; assigns a finite deadline; and forwards the opaque end-to-end commit body.

The responder verifies the commit HMAC derived from the challenge embedded in the signed candidate. It returns \textsc{Ready} through reverse labels. Relays change the matching pending mapping to \textsc{Active}. The initiator exposes the route to the data plane only after verifying the final ready proof. If either control message is lost, all partial state expires locally.

\section{Event lifecycle profile E1}
\label{sec:lifecycle}

\subsection{Half-open deadlines}

A local state created at $t_c$ with deadline $t_e$ is valid exactly on
\begin{equation}
[t_c,t_e)=\{t\mid t_c\le t<t_e\}.
\label{eq:halfopen}
\end{equation}
It is invalid at $t=t_e$ and cannot be revived. Received lifetime classes are converted to local deadlines; synchronized clocks are unnecessary. If a valid transition replaces a deadline, an old timer is associated with the previous state generation and cannot shorten the new state.

\subsection{Equal-time precedence}

Events assigned the same local timestamp follow the strict order
\begin{equation}
\mathsf{Expire}\prec\mathsf{Cancel}\prec\mathsf{RouteControl}
\prec\mathsf{Candidate}\prec\mathsf{Discover}\prec\mathsf{WindowClose}.
\label{eq:precedence}
\end{equation}
A message at its state deadline therefore fails, but a candidate at the exact window boundary remains eligible because candidate processing precedes window closure.

\subsection{Candidate windows and delayed responses}

A ring begins at local time $a_r$ and closes at $w_r=a_r+\Delta_r$. A candidate from any already-started ring is eligible at $w_r$ if no route has been selected, its signed offer has not expired, and every required tentative mapping survived the reverse traversal. Thus a delayed candidate from an earlier ring may win during a later ring without exposing a ring identifier on the wire.

Selection is immutable. After choosing one candidate, the initiator sends \textsc{Commit} on the selected path and sends transformed \textsc{Cancel} messages through stored adjacent mappings into maximal off-route subtrees that belong to its own discovery. Cancel is an optimization; expiry remains authoritative.

\subsection{Race examples}

\begin{table}[ht]
\centering
\caption{Representative E1 races}
\label{tab:races}
\begin{tabularx}{\textwidth}{@{}p{0.27\textwidth}p{0.23\textwidth}X@{}}
\toprule
Race & Winner & Result \\
\midrule
Candidate at window boundary & Candidate & Candidate is evaluated before the boundary closes. \\
State expiry and candidate & Expiry & State is invalid at the deadline; candidate is discarded. \\
Cancel before candidate at relay & Cancel & Branch context is removed; later candidate is discarded. \\
Candidate before cancel & Candidate transition & Tentative state survives under its own deadline or later abort. \\
Old tentative timer after COMMIT & New generation & Stale timer cannot shorten the pending-ready deadline. \\
Lost READY & Local expiry & Partial pending or active state is reclaimed without remote cooperation. \\
\bottomrule
\end{tabularx}
\end{table}

\section{Logical messages M1, wire cells W2, and relay state}
\label{sec:messages}

\subsection{Why the protocol separates messages from cells}

A cryptographic ciphertext normally reveals its complete byte length even when its contents are confidential. For an authenticated-encryption scheme with a fixed tag length $t$,
\begin{equation}
  |C|=|P_{\mathrm{padded}}|+t.
  \label{eq:cipher-length}
\end{equation}
Consequently, encrypting a variable-length message after adding a variable or random quantity of padding does not remove length leakage: the observer still learns the resulting padded length. Padding to one fixed maximum would hide length, but it would also make the logical-message grammar dependent on one transport allocation and would impose a hard maximum on nested candidates.

\ProtoName{} therefore uses two distinct encodings. M1 defines one canonical, variable-length byte string for each logical control message. M1 contains no semantic padding and no fragmentation metadata. W2 carries an M1 byte string in one or more equal-length adjacent-link cells. Fixed size belongs to the cell layer; logical structure belongs to the message layer.

\begin{property}[Layer separation]
Reassembly of a complete M1 message precedes message-type dispatch, C1 processing, and route-state allocation. A relay never cryptographically transforms a fragment in isolation and never forwards received fragment bytes or padding in place.
\end{property}

\subsection{M1 canonical logical-message envelope}

Let $M$ denote an M1 byte string and $B_M$ its message-specific body. The common envelope is shown in \Cref{tab:m1-envelope}. Profile identifiers and the message type remain inside adjacent-link encryption.

\begin{table}[ht]
\centering
\caption{M1 common logical-message envelope}
\label{tab:m1-envelope}
\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}p{0.14\textwidth}X@{}}
\toprule
Field & Length & Meaning \\
\midrule
Message type & 1 byte & \textsc{Chaff}, \textsc{Discover}, \textsc{Candidate}, \textsc{Commit}, \textsc{Ready}, \textsc{Cancel}, \textsc{Abort}, or \textsc{Close}. \\
Protocol version & 1 byte & Current protocol grammar. \\
Privacy profile & 1 byte & U1 identifier. \\
Lifecycle profile & 1 byte & E1 identifier. \\
Cryptographic suite & 2 bytes & C1 is \code{0x0001}; network byte order. \\
Message profile & 1 byte & M1 is \code{0x01}. \\
Reserved & 1 byte & MUST be zero. \\
Body length & VarUInt & Minimal unsigned base-128 LEB128 encoding. \\
Body & Variable & Exactly the declared bytes; no trailing bytes and no semantic padding. \\
\bottomrule
\end{tabularx}
\end{table}

If $\ell_v(x)$ denotes the number of octets in the minimal VarUInt encoding of $x$, then
\begin{equation}
  |M|=8+\ell_v(|B_M|)+|B_M|,
  \qquad 1\le |M|\le16{,}384.
  \label{eq:m1-length}
\end{equation}
The M1 decoder rejects non-minimal integers, unterminated integers, declared lengths beyond the containing message, trailing bytes, unknown mandatory profiles, and messages above the maximum. For example, zero is encoded as \code{00}; \code{80 00} is rejected as a second representation of the same value.

\begin{table}[ht]
\centering
\caption{Principal M1 message bodies}
\label{tab:m1-bodies}
\begin{tabularx}{\textwidth}{@{}p{0.17\textwidth}p{0.35\textwidth}X@{}}
\toprule
Message & Body fields & Size behavior \\
\midrule
\textsc{Chaff} & Empty & Nine-byte M1 message; one W2 cell. \\
\textsc{Discover} & Branch token, hop remaining, fan-out class, expiry class, options, reply public key, 128-byte eligibility capsule & Canonical 190-byte M1 message; one W2 cell. \\
\textsc{Candidate} & Candidate token, expiry class, layer count, nested-capsule length, nested C1 capsule & Variable; grows as reverse relays add authenticated layers. \\
Route control & Local label, four-byte generation, expiry class, protected-body length, protected body & Variable; protected body is bounded by 8,192 bytes. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{W2 fixed-size adjacent-link cell}

W2 uses one complete adjacent-link record length. Let $H_C$ be the public link header, $P_C$ the encrypted cell plaintext, and $T_C$ the authentication tag:
\begin{equation}
  \underbrace{H_C}_{12}
  \concat
  \underbrace{\operatorname{AEAD.Enc}_{K_\ell}(H_C,P_C)}_{1024+16}
  \quad\Longrightarrow\quad
  L_C=1052\ \text{bytes}.
  \label{eq:w2-record}
\end{equation}
The public header is $H_C=e_\ell\concat s_\ell$, where the epoch is a four-byte unsigned integer and the sequence is an eight-byte unsigned integer, both in network byte order. The 12 bytes are used as nonce and associated data. A link key and nonce pair MUST NOT be reused.

The first 32 plaintext bytes form the W2 fragment header; the remaining 992 bytes are fragment data followed by fresh random padding.

\begin{table}[ht]
\centering
\caption{W2 encrypted fragment header}
\label{tab:w2-header}
\begin{tabularx}{\textwidth}{@{}p{0.27\textwidth}p{0.13\textwidth}X@{}}
\toprule
Field & Length & Meaning \\
\midrule
W2, protocol, U1, E1 & 4 bytes & Wire and protocol profile identifiers. \\
C1 suite & 2 bytes & Cryptographic suite identifier. \\
Flags, reserved & 2 bytes & Both zero in this profile. \\
Local message identifier $\mu$ & 16 bytes & Non-zero random value scoped to one message, direction, link, and epoch. \\
Fragment index & 2 bytes & Zero-based index $j$. \\
Fragment count & 2 bytes & Canonical total $q(M)$. \\
Fragment length & 2 bytes & Number of M1 bytes in this cell. \\
Total M1 length & 2 bytes & Complete $|M|$, at most 16,384. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Canonical fragmentation}

Let $p=992$ be the fragment payload capacity. The only valid fragment count is
\begin{equation}
  q(M)=\left\lceil\frac{|M|}{p}\right\rceil,
  \qquad 1\le q(M)\le17.
  \label{eq:fragment-count}
\end{equation}
Every non-final fragment has length 992. The final fragment has length
\begin{equation}
  |M|-992\bigl(q(M)-1\bigr).
  \label{eq:final-fragment}
\end{equation}
A sender MUST NOT express the same M1 message as additional short fragments. The receiver rejects a noncanonical count or size even when concatenation could produce the declared bytes. This rule gives one W2 fragmentation representation for a given M1 message length.

A relay generates a new $\mu$ after reconstructing and transforming a logical message. The identifier is never copied across a hop and is never derived from a discovery, candidate, endpoint, route, or prior-hop field. Received padding is discarded; outgoing padding and link ciphertext are generated afresh for every cell.

\subsection{Bounded out-of-order reassembly}

A reassembly context is keyed by the authenticated directional link scope $\ell$ and local message identifier $\mu$. It stores immutable total-length and fragment-count metadata, a fragment bitmap, received bytes, a creation time, a half-open expiry, and a reservation equal to the declared logical-message length.

\begin{algorithm}[ht]
\caption{W2 receive, reassembly, and M1 admission}
\label{alg:reassembly}
\begin{algorithmic}[1]
\Require adjacent-link record $c$, peer/link scope $\ell$, local time $t$
\If{$|c|\ne1052$} \State \Return \textsc{Drop} \EndIf
\State authenticate and decrypt $c$ under $\ell$
\State parse and validate the W2 header and canonical fragment tuple
\State expire contexts whose deadline is $\le t$
\State $k\gets(\ell,\mu)$
\If{$k$ is absent}
  \State reserve one context and \code{total\_message\_length} bytes
  \State create $R[k]$ with immutable metadata and finite deadline
\ElsIf{metadata conflicts with $R[k]$}
  \State delete $R[k]$, release all reservations, \Return \textsc{Drop}
\EndIf
\If{fragment index already contains identical bytes}
  \State \Return \textsc{Duplicate}
\ElsIf{fragment index already contains different bytes}
  \State delete $R[k]$, release all reservations, \Return \textsc{Drop}
\EndIf
\State store the fragment
\If{some canonical index is absent} \State \Return \textsc{Collecting} \EndIf
\State $M\gets$ concatenation of fragments in index order
\State delete $R[k]$ and release fragment reservations
\State decode exactly one canonical M1 message from $M$
\If{M1 decoding or message-class admission fails} \State \Return \textsc{Drop} \EndIf
\State \Return the parsed message to its E1/C1 state machine
\end{algorithmic}
\end{algorithm}

An exact duplicate fragment is idempotent and consumes no new reservation. A conflicting duplicate or inconsistent metadata invalidates the entire context. An incomplete context expires locally without a response. Implementations MUST bound concurrent contexts, aggregate reserved logical bytes, maximum fragments per message, maximum message length, and timeout. Route-semantic state is not allocated while the message remains incomplete.

\subsection{Receive pipeline and control-message roles}

The normative receive order is:
\begin{enumerate}
  \item exact 1,052-byte cell-record length;
  \item public link epoch and sequence parsing, with an optional non-mutating replay-window precheck;
  \item adjacent-link AEAD authentication followed by replay-state commitment and authenticated exact-duplicate rejection;
  \item canonical W2 header and fragment validation;
  \item bounded reassembly, duplicate handling, and timeout;
  \item canonical M1 envelope and message-specific syntax;
  \item E1 lifetime, peer-rate, byte, state, and cryptographic-work admission;
  \item C1 point, URE, KEM, AEAD, signature, and transcript validation;
  \item branch or route-state transition;
  \item reconstruction of outgoing M1 messages and fresh W2 cells.
\end{enumerate}
A failure cannot consume resources reserved for a later stage. Detailed failure remains local and maps to bounded silent discard unless a separately specified non-amplifying response is required.

\begin{table}[ht]
\centering
\caption{Control-message role and state effect}
\label{tab:message-role}
\begin{tabularx}{\textwidth}{@{}p{0.16\textwidth}p{0.28\textwidth}X@{}}
\toprule
Message & Principal state effect & Privacy treatment \\
\midrule
\textsc{Discover} & Creates one bounded branch context & New branch token, reply-key tweak, URE rerandomization, reconstructed M1, fresh W2 ID and cells. \\
\textsc{Candidate} & Creates one tentative reverse mapping & New candidate token and label; nested authenticated C1 layer; canonical refragmentation. \\
\textsc{Commit} & Moves tentative state to \textsc{Pending-Ready} & Local labels and opaque end-to-end proof; no global candidate handle. \\
\textsc{Ready} & Activates the selected route & Reverse local label and opaque ready proof. \\
\textsc{Cancel} & Removes one off-route subtree context & Per-hop cancellation capability; no global route identifier. \\
\textsc{Abort}/\textsc{Close} & Reclaims setup or active state & Local route capability and bounded protected reason class. \\
\textsc{Chaff} & No route allocation & Ordinary M1/W2 path and scheduler; one-cell bounded parsing cost. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Relay state}

A branch context contains only local information: ingress peer and epoch, incoming branch capability, incoming reply key, eligibility capsule, expiry, bounded children, and the key material necessary for candidate return. It does not contain a source address, complete route, ring index, or logical-discovery identifier.

A tentative route mapping is separate from the branch context. It binds one parent candidate capability, one child candidate capability, one parent-facing forward label, one child-facing reverse label, one transcript digest, one generation, and one deadline. A valid \textsc{Commit} replaces the tentative deadline with a pending-ready deadline; a stale timer for the former generation cannot reclaim the new state. Only authenticated \textsc{Ready} changes it to active.

A W2 reassembly context is a third and strictly earlier state class. It is transport-local, contains no interpreted route semantics, and is deleted before the parsed M1 message is dispatched. This separation prevents an incomplete or conflicting fragment stream from manufacturing branch or route mappings.

\section{Unlinkability analysis}
\label{sec:unlinkability}

\subsection{Elimination of explicit equality handles}

Between an admitted incoming \textsc{Discover} and each outgoing child record, an honest relay changes:

\begin{enumerate}
  \item the branch token;
  \item the reply public key;
  \item all four URE group elements;
  \item the bounded propagation representation;
  \item the padding;
  \item the adjacent-link nonce and ciphertext;
  \item the output timing position within the declared batch policy.
\end{enumerate}

It also reconstructs the body rather than forwarding received bytes. Candidate tokens and route labels are likewise replaced on reverse and activation paths.

\begin{proposition}[No explicit stable handle]
Assume canonical C1 processing, independently sampled per-child randomness, and no implementation-added identifier. A conforming honest relay copies no protocol field unchanged from an incoming DISCOVER record into a non-adjacent outgoing DISCOVER record.
\end{proposition}

The proposition is syntactic. It does not imply that timing, topology, message scarcity, or maliciously chosen transformations cannot correlate the records.

\subsection{Conditional batch-local argument}

Let an honest relay release a batch of $k$ records with identical observable class and scheduling treatment. If the C1 reply-key transformation and URE rerandomization are unlinkable under their assumptions, link ciphertexts are fresh, and no hidden implementation field survives, then a passive observer's matching information is limited to cryptographic advantage and batch-policy information. In the ideal uniform batch, the baseline guessing probability is $1/k$.

The argument fails when $k=1$, when one logical message has a distinguishable cell count or release pattern, when a stable logging or tracing field leaks, or when the observer uses end-to-end timing. Accordingly, U1 conformance is profile-specific rather than a property of C1 alone.

\subsection{Persistent active ratio tag}
\label{sec:active-tagging}

The passive transformation argument does not extend to a compromised relay. In a valid C1 consistency pair,
\begin{equation}
(U_1,V_1)=(r_1A,r_1B), \qquad A=aB.
\label{eq:validconsistency}
\end{equation}
A malicious relay chooses a known non-zero scalar $c$ and replaces the pair by
\begin{equation}
(\widetilde U_1,\widetilde V_1)=(cV_1,V_1).
\label{eq:ratiotag}
\end{equation}
Both values remain canonical non-identity group elements. After an honest relay rerandomizes with non-zero scalar $s$, the pair becomes
\begin{equation}
(\widetilde U'_1,\widetilde V'_1)=(scV_1,sV_1).
\end{equation}
A colluding downstream relay tests
\begin{equation}
\widetilde U'_1\stackrel{?}=c\widetilde V'_1,
\label{eq:tagtest}
\end{equation}
which always holds. The relation is therefore a deterministic cross-hop tag that survives the honest transformation.

The destination computes the consistency value
\begin{equation}
T=\widetilde U'_1-a\widetilde V'_1=(c-a)sV_1.
\label{eq:tagfailure}
\end{equation}
Except when $c=a$, the destination rejects the capsule and returns no candidate. The attack combines linkability and denial of service; observation of the missing candidate or missing route activation may also provide confirmation.

\begin{proposition}[Active-unlinkability counterexample]
Under the present C1 eligibility construction, two colluding compromised relays separated by any number of honest rerandomizing relays can link a tagged branch by the test in \Cref{eq:tagtest}. Consequently, the protocol does not satisfy active-adversary message unlinkability.
\end{proposition}

Adjacent-link authentication does not prevent the attack. It rejects modification by a third party on an edge, but a compromised relay can originate new syntactically valid outgoing M1/W2 traffic containing the tagged pair. Fixed cell length, canonical M1 and point encoding, non-identity checks, fresh link encryption, and generic endpoint failure are necessary controls but do not erase the algebraic relation.

The active-security gate remains closed until a replacement destination-eligibility mechanism has a precise active-adversary definition, reviewed security argument, bounded verification cost, fixed-size encoding, and negative tests for algebraic, substitution, replay, truncation, and selective-failure tags.

\subsection{Timing limitation}

Fixed record sizes and per-hop cryptographic changes do not hide timing. TARANET's coordinated constant-rate approach and Loopix's stochastic mixing with cover traffic represent different latency--bandwidth trade-offs~\cite{taranet,loopix}. \ProtoName{} treats batching, release cadence, and cover traffic as separate scheduling profiles. Each profile must state its observer, latency, bandwidth cost, minimum anonymity set, and measured matching success.

\section{Resource and denial-of-service model}
\label{sec:resources}

\subsection{Why privacy increases state}

Without a shared attempt identifier, a relay cannot recognize all converging copies of one logical discovery. In a tree approximation with relay fan-out $f$ and depth $d$, the number of branch transmissions can approach
\begin{equation}
T(f,d)=\sum_{i=1}^{d}f^i=\frac{f^{d+1}-f}{f-1}, \qquad f>1.
\label{eq:treegrowth}
\end{equation}
Cycles and duplicate graph paths can increase repeated visits further. Privacy therefore requires hard limits rather than optimistic assumptions about deduplication.

\subsection{Required limits}

Every node defines finite limits for:

\begin{itemize}
  \item accepted branch contexts per ingress peer and globally;
  \item fresh branches per peer and time window;
  \item outgoing children per branch and total queued W2 cells;
  \item complete M1 message length, protected-body length, and fragments per message;
  \item concurrent W2 reassembly contexts and aggregate reserved logical bytes;
  \item fragment residence time, duplicate processing, and conflicting-fragment cleanup;
  \item URE rerandomizations, group multiplications, KEM operations, and signature checks;
  \item candidate responses per branch and per endpoint identity;
  \item tentative, pending-ready, and active route capacity;
  \item logical-message count, cell transmissions, setup duration, retries, and cleanup retention.
\end{itemize}


Authenticated exact cell replay rejection occurs before route-level token-bucket consumption. A receiver may reject obviously stale public sequence values provisionally, but it advances the replay window only after link authentication. A fresh valid branch token is not a replay and consumes an ingress token before expensive route cryptography. Reassembly admission is bounded separately and occurs before route-semantic or public-key work. Per-peer buckets mitigate concentrated sources but do not provide Sybil or distributed-denial resistance.

\subsection{Amplification condition}

For one admitted branch $b$, define
\begin{equation}
\alpha_b=\frac{W_{\mathrm{out}}(b)+W_{\mathrm{state}}(b)+W_{\mathrm{crypto}}(b)}{W_{\mathrm{in}}(b)},
\end{equation}
where the numerator represents normalized outgoing, state, and cryptographic work. A profile must bound each component independently. A small response byte count does not compensate for unbounded group operations or retained state.

\section{Deterministic evaluation}
\label{sec:evaluation}

\subsection{Method}

The evaluation uses deterministic graph and event models rather than a packet-level network benchmark. The experiments use reproducible seeds, explicit topology size and average degree, responder density, fan-out, hop limits, state and transmission budgets, link delay, loss, exact duplication, and attacker generation. Results quantify design behavior under the model; they do not estimate Internet latency or prove security.

The C1 reference implementation uses \code{libsodium} group operations and the Python \code{cryptography} implementations of Ed25519 and ChaCha20-Poly1305. Deterministic vectors cover URE encryption and rerandomization, wrong-key rejection, reply-key tweak consistency, KEM/AEAD opening, tamper and context mismatch, descriptor/address derivation, and candidate signature binding.

\subsection{U1 branch-local cost}

\begin{table}[ht]
\centering
\caption{Identifier-based discovery versus U1 branch-local processing on 500-node graphs}
\label{tab:u1cost}
{\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.19\textwidth}>{\centering\arraybackslash}p{0.105\textwidth}>{\centering\arraybackslash}p{0.115\textwidth}>{\centering\arraybackslash}p{0.085\textwidth}>{\centering\arraybackslash}p{0.105\textwidth}X@{}}
\toprule
Parameters & Tx ovh. & State ovh. & Success & Budget hit & Reading \\
\midrule
Hop 3, fan-out 2 & 0.43\% & 1.60\% & 23\% & 0\% & Low cost; insufficient reach. \\
Hop 4, fan-out 3 & 4.26\% & 15.60\% & 85\% & 0\% & Conservative baseline. \\
Hop 4, fan-out 4 & 8.95\% & 42.78\% & 100\% & 0\% & Full success with higher state. \\
Hop 5, fan-out 4 & 31.90\% & 180.88\% & 100\% & 91\% & Broad flooding is unsuitable. \\
\bottomrule
\end{tabularx}}
\end{table}

The result supports expanding rings: begin with a narrow branch budget and expand only when the candidate set remains insufficient. It also shows why the unlinkability mechanism cannot be evaluated only by successful route probability.

\subsection{E1 lifecycle model}

The tracked event experiment uses 100 deterministic 500-node graphs with average degree eight and two-percent responders. The ring schedule is $(2,2,18\,\mathrm{ms})$, $(3,2,24\,\mathrm{ms})$, and $(4,3,32\,\mathrm{ms})$, where each tuple is maximum depth, fan-out, and candidate-window duration. Branch capacity is 1,200 and the total transmission budget is 2,000.

\begin{table}[ht]
\centering
\caption{Event-lifecycle comparison on 500-node graphs}
\label{tab:lifecycle}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xrrrrrr@{}}
\toprule
Scenario & Success & Setup ms & Total tx & Attack tx & Peak branch & Cleanup \\
\midrule
Clean & 0.89 & 72.60 & 212.77 & 0.00 & 103.92 & 1.00 \\
2\% loss, 5\% duplicate & 0.80 & 72.89 & 207.68 & 0.00 & 96.92 & 1.00 \\
Fresh-branch attack, open & 0.32 & 55.13 & 1440.33 & 1389.94 & 1140.38 & 1.00 \\
Fresh-branch attack, bucket & 0.76 & 66.93 & 1176.56 & 1037.09 & 904.67 & 1.00 \\
\bottomrule
\end{tabularx}
\end{table}

The token bucket raises attack-scenario success from 32\% to 76\%, reduces attack transmissions by 25.4\%, and reduces peak branch state by 20.7\%. It does not restore clean behavior. The lower conditional setup latency in the unrestricted attack is survivor bias: only easier successful routes contribute to the mean. Every scenario reaches zero final branch, offer, candidate, tentative, pending, and active state.

\subsection{C1 conformance evidence}

\begin{table}[ht]
\centering
\caption{C1 executable checks}
\label{tab:c1tests}
\begin{tabularx}{\textwidth}{@{}p{0.30\textwidth}X p{0.13\textwidth}@{}}
\toprule
Check & Expected property & Result \\
\midrule
URE rerandomization & Ciphertext bytes change; decrypted marker remains identical. & Pass \\
Wrong eligibility key & Wrong endpoint does not recognize the marker. & Pass \\
Reply public/secret tweak & $(x+\delta)B=X+\delta B$. & Pass \\
Reply KEM and AEAD & Correct context opens; tamper, AAD, and info mismatch fail generically. & Pass \\
Candidate signature & Any transcript-field change invalidates the Ed25519 signature. & Pass \\
Vector reproducibility & Generated JSON equals the tracked canonical vector file. & Pass \\
Complete deterministic suite & 61 unit, lifecycle, message, cell, reassembly, cryptographic, and tagging tests. & Pass \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{M1 logical size and W2 cell capacity}

The first integrated codec experiment constructs actual C1 candidate chains and measures their canonical M1 length and W2 cell count. The responder contributes one innermost layer; every reverse relay adds one 115-byte authenticated wrapper in the current C1 encoding. \Cref{tab:capacity} shows representative boundaries.

\begin{table}[ht]
\centering
\caption{Selected M1/W2 capacity boundaries for nested candidates}
\label{tab:capacity}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xrrrr@{}}
\toprule
Message & Relay wrappers & C1 layers & M1 bytes & W2 cells \\
\midrule
\textsc{Discover} & 0 & -- & 190 & 1 \\
\textsc{Candidate} & 0 & 1 & 334 & 1 \\
\textsc{Candidate} & 5 & 6 & 909 & 1 \\
\textsc{Candidate} & 6 & 7 & 1,024 & 2 \\
\textsc{Candidate} & 11 & 12 & 1,599 & 2 \\
\textsc{Candidate} & 15 & 16 & 2,059 & 3 \\
\bottomrule
\end{tabularx}
\end{table}

The transition at seven candidate layers illustrates the reason for separating messages from cells. The candidate is a valid 1,024-byte logical message, but only 992 message bytes fit in one W2 cell after the encrypted fragmentation header. W2 therefore emits two equal-size cells rather than rejecting the route or introducing a second public record length. The protocol maximum of 16,384 M1 bytes and 17 cells remains an explicit implementation and resource bound, not an implicit consequence of one record layout.

\subsection{Integrated fragmentation and cell-loss experiment}

A second experiment uses deterministic line topologies with one eligible responder at the far endpoint. Every scenario executes C1 transformations, M1 encoding, W2 link authentication, out-of-order reassembly, \textsc{Candidate}, \textsc{Commit}, \textsc{Ready}, expiry, and final cleanup. Forty runs are performed for each route depth and transport condition.

\begin{table}[ht]
\centering
\caption{Route depth, fragmentation, and independent two-percent cell loss}
\label{tab:fragment-life}
{\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.16\textwidth}rrrrrr@{}}
\toprule
Condition & Hops & Success & Logical msgs & W2 cells & Fragmented msgs & Cleanup \\
\midrule
Clean & 2 & 1.00 & 8.00 & 8.00 & 0.00 & 1.00 \\
Clean & 5 & 1.00 & 20.00 & 20.00 & 0.00 & 1.00 \\
Clean & 8 & 1.00 & 32.00 & 34.00 & 2.00 & 1.00 \\
Clean & 12 & 1.00 & 48.00 & 54.00 & 6.00 & 1.00 \\
2\% cell loss & 2 & 0.80 & 7.53 & 7.53 & 0.00 & 1.00 \\
2\% cell loss & 5 & 0.65 & 16.43 & 16.43 & 0.00 & 1.00 \\
2\% cell loss & 8 & 0.50 & 25.55 & 27.15 & 1.60 & 1.00 \\
2\% cell loss & 12 & 0.30 & 30.83 & 34.30 & 3.48 & 1.00 \\
\bottomrule
\end{tabularx}}
\end{table}

In clean transport, fragmentation removes the former setup-depth discontinuity: all evaluated routes activate and later reclaim every branch, reassembly, tentative, pending, and active state. At eight hops the protocol sends two additional cells for two fragmented messages; at twelve hops it sends six additional cells. Under independent cell loss, success falls with both route length and cell count because completion requires every indispensable fragment and route-control cell. The model intentionally contains no retransmission profile. The result therefore identifies the next reliability question rather than measuring a final deployment.

Peak reserved reassembly bytes grow from 449 bytes at two hops to 1,599 bytes at twelve hops in clean runs. The value is the largest declared M1 message concurrently retained in this line experiment, not the profile maximum. All evaluated runs finish with zero reassembly contexts, including the loss cases. This confirms bounded cleanup under the modeled timeout but does not establish resistance to a coordinated fragment-spray attacker.

\subsection{Integrated C1 active-tagging experiment}

A separate five-node line gives every cryptographic transformation an unambiguous expected count. Forty deterministic runs are executed per scenario. The clean route emits 16 W2 cells, corresponding to 16,832 complete wire bytes; performs three forward URE/reply-key transformations; opens four nested candidate layers; authenticates \textsc{Commit} and \textsc{Ready}; and finishes with zero residual state.

\begin{table}[ht]
\centering
\caption{Integrated link-authentication and active-tagging comparison}
\label{tab:integrated}
{\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xrrrrr@{}}
\toprule
Scenario & Success & Mean cells & Mean wire bytes & Crypto fail & Cleanup \\
\midrule
Clean integrated path & 1.000 & 16.000 & 16,832.0 & 0.000 & 1.000 \\
2\% adjacent-link tampering & 0.725 & 13.850 & 14,570.2 & 0.000 & 1.000 \\
Single ratio-tagging relay & 0.000 & 8.000 & 8,416.0 & 1.000 & 1.000 \\
Colluding ratio-tagging relays & 0.000 & 8.000 & 8,416.0 & 1.000 & 1.000 \\
\bottomrule
\end{tabularx}}
\end{table}

The tampering scenario records a mean of 0.3 adjacent-link authentication failures per run and never allocates route state from a rejected cell. In the single-tag scenario the destination rejects the capsule. In the colluding scenario the downstream compromised relay records one successful ratio observation per run before destination rejection. The attack changes algebraic content, not cell length or M1 syntax. These results demonstrate the executable mechanism and failure path; they do not quantify detectability in an arbitrary topology or prove security of any replacement construction.

\section{Limitations and open problems}
\label{sec:limitations}

\subsection{Eligibility construction and active security}

The ratio-tag counterexample is the principal blocking defect. Correct URE rerandomization and passive ciphertext anonymity are insufficient when a relay can deliberately install a homogeneous relation that an honest transformation preserves. A replacement must hide the destination key, permit public per-hop transformation, resist persistent tagging and selective failure, fit bounded M1 messages and fixed W2 cells, and have bounded verification cost. Detection only at the destination is insufficient because colluding relays have already linked the branch.

\subsection{Independent cryptographic review}

The remaining C1 equations are executable and internally consistent, but correctness is not equivalent to security. Review must address recipient anonymity, the additive related-key behavior of the custom reply KEM, CCA behavior of nested layers, transcript and key privacy, point-encoding assumptions, failure timing, and side-channel behavior. The custom reply construction is HPKE-inspired and is not an RFC~9180 HPKE suite.

\subsection{Fragmentation, reliability, and cell-count leakage}

M1/W2 removes a hard one-record candidate-depth limit, but it does not make message size invisible. A passive adjacent-link observer can count the cells in a burst and may infer a range for candidate depth or protected-body size. A traffic-scheduling profile must decide whether to interleave fragments, pad cell counts to declared classes, delay completion to a release schedule, or accept this leakage explicitly.

Multi-cell messages are also more exposed to loss. If a message uses $q$ indispensable cells and independent cell delivery succeeds with probability $p$, message delivery without retransmission is approximately $p^q$. Real losses are not independent, and a route setup contains several messages, but the expression explains the observed direction of the effect. Bounded retransmission, forward-error correction, or redundant scheduling must be designed without adding a stable cross-hop message identifier or an amplification channel.

Reassembly creates a distinct denial-of-service surface. The fixed limits on complete message length, fragment count, concurrent contexts, aggregate reserved bytes, and timeout are normative. Further work must evaluate authenticated fragment sprays, peer rotation, deliberately missing final fragments, conflicting duplicates, and eviction fairness.

\subsection{Post-quantum migration}

C1 is entirely classical. A hybrid KEM replacement is not enough because the eligibility selector must also preserve destination-key privacy and safe public transformation. Post-quantum rerandomization or a different private eligibility mechanism is therefore a separate research problem.

\subsection{Directory and descriptor distribution}

Long-range descriptor resolution is outside the protocol defined here. A future directory must provide authenticated descriptor binding, freshness, replication, poisoning resistance, selective-denial analysis, enumeration resistance, and private queries. Repeated deterministic hashing of an endpoint address is not sufficient query privacy.

\subsection{Distributed abuse and fairness}

Per-peer token buckets remain vulnerable to Sybil distribution. Candidate and responder spam require quotas independent of branch admission. Evaluation must vary attacker distribution, peer churn, adaptive refill, honest convergence, and fairness by ingress peer rather than report only aggregate success.

\subsection{Traffic privacy}

Timing, direction, volume, cell count, fragment spacing, and topology remain major correlation surfaces. A traffic-privacy profile must define batching, fragment interleaving, cell-count padding, release cadence, cover traffic, overflow behavior, and observable classes, then measure bandwidth, latency, reassembly reliability, and matching success against a named adversary. Encryption and fixed cell length alone do not imply logical-message or traffic-flow unlinkability.

\section{Implementation and review strategy}
\label{sec:strategy}

Development should proceed through explicit gates:

\begin{enumerate}
  \item replace or strengthen the eligibility construction against the ratio-tag and selective-failure class;
  \item state a precise active-adversary game and obtain independent review of the eligibility and reply-key mechanisms;
  \item define M1 and W2 in a reviewed machine-readable schema and implement an independent second codec;
  \item cross-check minimal VarUInt encodings, message boundaries, canonical fragmentation, conflict handling, and C1 vectors across implementations;
  \item fuzz malformed M1 lengths, W2 headers, fragment order, duplicate conflicts, message identifiers, nested candidate boundaries, point encodings, transcript substitutions, and state races;
  \item model fragment-spray attacks and verify hard limits on contexts, bytes, fragments, cryptographic work, and expiry processing;
  \item define bounded message repair or retransmission without stable cross-hop identifiers or unbounded amplification;
  \item build a user-space overlay prototype over an existing authenticated transport;
  \item test interoperability under loss, duplication, reordering, partial messages, link churn, malicious peers, and distributed resource pressure;
  \item specify cell interleaving, cell-count padding, and traffic-scheduling profiles before making message-size, timing, or flow claims;
  \item design long-range descriptor resolution as a separate protocol with its own threat model.
\end{enumerate}

The first overlay prototype should use ordinary IP connectivity only as an experimental transport. It should not require special routers or a new link technology. Protocol state, cryptographic transformations, message/cell interoperability, failure behavior, and resource limits should be evaluated before lower-layer integration is considered.

\section{Conclusion}
\label{sec:conclusion}

\ProtoName{} defines a privacy-enabled route-discovery control plane rather than a replacement for IP or BGP. Its current form has a precise operational boundary. U1 removes explicit cross-hop equality handles under passive observation; E1 gives deterministic bounded route setup and cleanup; C1 defines concrete classical cryptographic operations and transcripts; M1 gives each logical control operation one canonical variable-length representation; and W2 carries those messages in independently authenticated fixed-size adjacent-link cells with bounded reassembly.

The separation between M1 and W2 resolves a layering defect. Fixed width is retained where it hides an observable transmission-unit length, while the protocol grammar remains compact and extensible. Deep candidates can span several cells instead of failing at a one-record boundary. The cost is explicit: cell count and timing may reveal a message-size class, multi-cell messages have greater loss exposure, and reassembly requires additional bounded state.

The integrated model shows that cryptography, canonical encoding, fragmentation, reassembly, and lifecycle can be exercised together. Clean routes activate only after authenticated candidate, \textsc{Commit}, and \textsc{Ready} processing; malformed or unauthenticated cells fail before route-state allocation; incomplete messages expire; and every evaluated scenario reclaims all state. The same integration preserves a material security warning. The C1 eligibility consistency pair admits a persistent ratio tag, so active-adversary message unlinkability is not achieved.

The protocol is therefore suitable as a falsifiable research specification and interoperability target, but not as a production anonymity system. The next gates are an active-tag-resistant eligibility mechanism, an independent M1/W2 implementation with fuzzing, a bounded reliability profile for multi-cell messages, and a traffic scheduler that explicitly addresses cell-count and timing leakage.

\appendix
\section{Normative requirement summary}
\label{app:normative}

The keywords ``MUST'', ``MUST NOT'', ``REQUIRED'', ``SHOULD'', ``SHOULD NOT'', and ``MAY'' are interpreted according to BCP~14~\cite{rfc2119,rfc8174}.

An implementation claiming U1, E1, C1, M1, and W2 conformance MUST:

\begin{enumerate}
  \item omit logical discovery, attempt, ring, global candidate, global route, and prior-hop W2 message identifiers from outgoing messages;
  \item replace branch, candidate, cancellation, route, and W2 local-message capabilities at every hop;
  \item use independent reply root keys and URE randomness for every first-hop branch;
  \item validate canonical RFC~9496 point encodings and C1 scalar encodings;
  \item reject identity public keys and KEM encapsulations;
  \item apply the exact C1 URE encryption, rerandomization, and eligibility equations;
  \item use independent rerandomization scalars with $s_0\ne0$ and $s_1\notin\{0,1\}$ for every child;
  \item apply the exact C1 reply-key tweak and \code{TR-KEM-R255} context derivation;
  \item bind candidate fields in the exact ordered transcript and verify Ed25519 under the descriptor key;
  \item map cryptographic failures to one external state-machine result;
  \item encode every logical control operation as one canonical M1 message with a minimal body length, exact containment, no trailing bytes, and no semantic padding;
  \item enforce $1\le |M|\le16{,}384$ and the 8,192-byte protected route-control bound;
  \item fragment each M1 message canonically into $q(M)=\lceil |M|/992\rceil$ W2 cells, with at most 17 cells;
  \item encode every W2 transmission as exactly 1,052 bytes, advance link replay state only after adjacent-link authentication, authenticate the fragment header, discard received padding, and use fresh padding, nonce, sequence, and link ciphertext for every outgoing cell;
  \item bind reassembly to authenticated link direction and a fresh non-zero local message identifier;
  \item accept out-of-order fragments, process exact duplicates idempotently, invalidate conflicting duplicates, and reject inconsistent metadata;
  \item bound concurrent reassemblies, aggregate reserved logical bytes, fragment count, message length, and reassembly timeout;
  \item allocate no branch, candidate, tentative, pending, or active route state before complete W2 reassembly and canonical M1 decoding;
  \item enforce finite per-peer, node-global, time, logical-byte, cell, state, queue, and cryptographic-work limits;
  \item treat state lifetimes as half-open intervals and follow E1 equal-time precedence;
  \item create tentative state on \textsc{Candidate}, reserve pending state on \textsc{Commit}, and authorize data only after final \textsc{Ready};
  \item reclaim all reassembly and setup state by local expiry without remote cooperation;
  \item make batch-local unlinkability claims only for a declared cell interleaving, batching, and observable cell-count profile;
  \item refrain from active-adversary, traffic-flow, post-quantum, or production-security claims until separately reviewed and tested;
  \item expose no detailed error behavior that distinguishes tagged eligibility failure from other invalid cryptographic, message, cell, or reassembly input.
\end{enumerate}

\section{C1 vector summary}
\label{app:vectors}

The supplementary canonical vector set fixes the C1 interoperability values. The following values identify that set:

\begin{table}[ht]
\centering
\caption{Selected deterministic C1 vector values}
\label{tab:vectors}
\begin{tabularx}{\textwidth}{@{}p{0.26\textwidth}X@{}}
\toprule
Field & Hexadecimal value \\
\midrule
Suite identifier & \code{0001} \\
Endpoint address & {\ttfamily\small\seqsplit{7df7ebca17b539f1e08857046a44099f4eba764d670eed5ca84ff7a265d60deb}} \\
Eligibility marker & {\ttfamily\small\seqsplit{72cffa1d9862e7f4f9b21445da83343808936f19972da0cdd41d422ca991de5e}} \\
Root reply public key & {\ttfamily\small\seqsplit{befb53cee99672e930a9548a1666cf20aa218f314145c7474d609b19375eae16}} \\
Tweaked reply public key & {\ttfamily\small\seqsplit{be63f1a19d7c1c1079ccc921b3a39957495e62c71c3e40531dd8095f08384055}} \\
Candidate transcript hash & {\ttfamily\small\seqsplit{32703335d293a706b97a9beb49f4d89be00aace280a7cd1b085368a90d3bf833}} \\
\bottomrule
\end{tabularx}
\end{table}

The vector generator uses public deterministic labels to make results reproducible. These labels and derived scalars MUST NOT be used in an operational deployment.

\section{Canonical message and cell encoding note}

M1 uses minimal unsigned base-128 LEB128 for body and nested variable lengths. Every decoder MUST reject a longer encoding of a value, an unterminated integer, a value above its field-specific maximum, trailing bytes, or a nested length that exceeds its containing message. Numeric fields declared fixed-width by C1 or a message body remain unsigned big-endian values.

W2 uses fixed-width unsigned big-endian integers in the public 12-byte link header and encrypted 32-byte fragment header. The fragment count is not freely selected: it is the unique value $\lceil |M|/992\rceil$. Every non-final fragment carries exactly 992 M1 bytes and the final fragment carries exactly the remainder. Alternative short-fragment encodings of the same message are invalid.

The fixed 1,052-byte W2 record length hides exact per-cell content length. It does not hide the number, direction, or timing of cells. These observables belong to a separately declared scheduling profile.

\begin{thebibliography}{99}

\bibitem{rfc2119}
S. Bradner,
``Key words for use in RFCs to Indicate Requirement Levels,''
BCP 14, RFC 2119, March 1997.

\bibitem{rfc8174}
B. Leiba,
``Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words,''
BCP 14, RFC 8174, May 2017.

\bibitem{sphinx}
G. Danezis and I. Goldberg,
``Sphinx: A Compact and Provably Secure Mix Format,''
in \emph{IEEE Symposium on Security and Privacy}, 2009.

\bibitem{hornet}
C. Chen, D. E. Asoni, D. Barrera, G. Danezis, and A. Perrig,
``HORNET: High-speed Onion Routing at the Network Layer,''
arXiv:1507.05724, 2015.

\bibitem{loopix}
A. M. Piotrowska, J. Hayes, T. Elahi, S. Meiser, and G. Danezis,
``The Loopix Anonymity System,''
in \emph{26th USENIX Security Symposium}, 2017, pp. 1199--1216.

\bibitem{taranet}
C. Chen, D. E. Asoni, A. Perrig, D. Barrera, G. Danezis, and C. Troncoso,
``TARANET: Traffic-Analysis Resistant Anonymity at the Network Layer,''
in \emph{IEEE European Symposium on Security and Privacy}, 2018, pp. 137--152.

\bibitem{gjjs}
P. Golle, M. Jakobsson, A. Juels, and P. Syverson,
``Universal Re-encryption for Mixnets,''
in \emph{Topics in Cryptology -- CT-RSA 2004}, LNCS 2964, 2004, pp. 163--178.

\bibitem{ure2023}
F. Banfi, U. Maurer, and S. Ritsch,
``On the Security of Universal Re-Encryption,''
Cryptology ePrint Archive, Paper 2023/1165, 2023.

\bibitem{hpke}
R. Barnes, K. Bhargavan, B. Lipp, and C. A. Wood,
``Hybrid Public Key Encryption,''
RFC 9180, February 2022.

\bibitem{ristretto}
H. de Valence, J. Grigg, M. Hamburg, I. Lovecruft, G. Tankersley, and F. Valsorda,
``The ristretto255 and decaf448 Groups,''
RFC 9496, December 2023.

\bibitem{hkdf}
H. Krawczyk and P. Eronen,
``HMAC-based Extract-and-Expand Key Derivation Function (HKDF),''
RFC 5869, May 2010.

\bibitem{chacha}
Y. Nir and A. Langley,
``ChaCha20 and Poly1305 for IETF Protocols,''
RFC 8439, June 2018.

\bibitem{eddsa}
S. Josefsson and I. Liusvaara,
``Edwards-Curve Digital Signature Algorithm (EdDSA),''
RFC 8032, January 2017.

\end{thebibliography}

\end{document}
