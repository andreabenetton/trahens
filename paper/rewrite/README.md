# Paper rewrite

`main.tex` is the first clean rewrite. It is intentionally shorter than the legacy draft and follows the current repository specification rather than copying incomplete algorithms.

Build with:

```bash
make paper
```

The rewrite should remain descriptive until the protocol semantics, cryptographic profile, and experiments are stable. Normative behavior belongs in `spec/`.
