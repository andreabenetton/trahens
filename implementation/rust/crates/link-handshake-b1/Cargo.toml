[package]
name = "link-handshake-b1"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
protocol-registry = { path = "../protocol-registry" }
trahens-crypto = { path = "../crypto" }

[lints]
workspace = true

[dev-dependencies]
test-vectors = { path = "../test-vectors" }
hex = "0.4"
# The independent Noise cross-check lives in ../../crosscheck, outside the
# workspace: snow's dependency tree exceeds this workspace's build floor, and
# excluding it keeps that floor and the libsodium-only posture intact.
